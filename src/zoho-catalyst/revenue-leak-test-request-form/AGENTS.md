# RevenueLeakTestRequestForm Instructions

- Production remains blocked in source. A Development test, merge, or upload does not authorize Production.
- `RevenueLeakTestRequestForm` and `RevenueLeakTestSetupForm` target the existing Retell Catalyst project in Development, but they remain separate functions with separate routes, variables, artifacts, and rollback paths. Form 1 uses only its exact session table and CRM Connections. Never use an unscoped deployment that can change another Retell-project resource.
- Historical readback proves the target session table and two Form 1 CRM Connections exist, but it does not prove this source candidate is installed. Preserve predecessor assets as disabled evidence until replacement installation and E2E acceptance pass; cleanup requires separately verified dependency and row readback.
- Never put CRM IDs, names, email addresses, phone numbers, raw tokens, secrets, or private routes in Git, logs, or the Form URL.
- Form 1's existing native CRM upsert remains the only public submission writer. The assisted lane is mutually exclusive: Catalyst validates server-resolved `prefillId`, submission identity, and immutable revision and updates only the server-bound CRM record; browser- or form-supplied CRM or journey identity is ignored.
- Issue, Access, Exchange, Prefill, and Submission must validate the exact Development project, route, and method. Issue, Prefill, and Submission additionally validate exact media type and private server-caller authentication before body parsing, SDK access, Data Store access, CRM Connection use, or outbound I/O.
- Never expose a raw CRM ID, PII, configuration value, or reusable credential in a Form URL. The real journey credential is fragment-only and exchanged through same-origin `POST`; Zoho Forms receives only an independent approximately ten-minute prefill handle. Store only domain-separated digests, bind the exact organization, record, journey, form, stage, and immutable source revision, consume the handle after successful prefill, and consume the assisted session only after exact CRM readback.
- Keep predecessor CRM controls available until replacement E2E acceptance. The five assisted routes, dedicated prefill-handle alias, and writer suppression remain live-disabled until authoritative Forms and Catalyst readback proves the manifest; rollback unbinds only the replacement callers and preserves evidence.
- Stamp `lib/source-revision.js` only in a temporary artifact built from a clean, reviewed commit.
- Use synthetic fixtures only. Run the direct syntax checks, Node test suite, and repository verifier before handoff.
