"use strict";

// Deployment tooling must replace this sentinel with the exact reviewed Git
// commit. Because it is not a 40-character hash, unstamped source fails closed.
const ARTIFACT_SOURCE_REVISION = "__SYLVARA_UNSTAMPED_SOURCE_REVISION__";

module.exports = { ARTIFACT_SOURCE_REVISION };
