"use strict";

// A reviewed immutable export must replace this sentinel. Runtime environment
// metadata cannot claim a source revision that is absent from the artifact.
const ARTIFACT_SOURCE_REVISION = "__SYLVARA_UNSTAMPED_SOURCE_REVISION__";

module.exports = { ARTIFACT_SOURCE_REVISION };
