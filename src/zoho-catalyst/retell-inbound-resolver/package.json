{
  "name": "@sylvara/retell-inbound-resolver-contract",
  "version": "1.0.0",
  "private": true,
  "description": "Fail-closed Sylvara coverage-mode mapping and trigger compatibility contract.",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "ci": "node --test test/*.test.js",
    "test": "node --test test/*.test.js"
  }
}
