# Free Revenue Leak Test CRM Controls

This directory is the sanitized, version-controlled desired state for the Free Revenue Leak Test lifecycle in Zoho CRM. CRM remains the relationship, qualification, commercial-acceptance, and summary system. Catalyst owns call events, deployment ownership, notification processing, counters, and detailed reports.

[`config/automation-contract.json`](config/automation-contract.json) preserves the sanitized count-only 2026-08-24 audit of 17 Deal fields and now references a separate field-by-field 2026-08-28 read-only preflight for the exact 25-field gap derived from the immutable 2026-08-14 snapshots. The fresh evidence proves that every gap field is currently present, visible, writable, API-create/update enabled, active-layout associated, transition-available, and compatible with the required data type and immutable picklist API values. It does not rewrite the historical snapshots or substitute a self-authored field union. The contract also records four logical workflows, the single Revenue Desk Blueprint, distinct version-specific approval and post-route-readback activation invariants, paid-acceptance gates, and the disposable `ZZZ SYNTHETIC` acceptance path. The control-field Deal initializer remains an explicit provider-safe 5+3 split: Controls owns five field updates and Limits owns three. A separate create-only function owns only `Setup_Access_Issue_Request_ID`, persists one UUID-v4-shaped non-secret idempotency identifier, suppresses recursive CRM automation, and requires exact readback before the Form 2 button may use it. The access capability itself remains HMAC-derived inside Catalyst. Approval leaves the Deal's test status `Scheduled` and its actual timing empty; activation alone may set `Live` after Catalyst validates the chained receipt. For migration safety, the CRM display value remains `7-Day Revenue Leak Test` while its actual/reference value and the mapped Zoho Forms choice remain `Free 7-Day Missed-Call`; independently read-back workflow and Blueprint criteria use the display value, and every customer-facing label uses **Free Revenue Leak Test**.

The Blueprint topology separates controller/provider evidence that must already exist from operator inputs collected during a manual transition. Form 2 proof is controller-owned: the one-time proof must be consumed and the exact controller receipt plus CRM submission evidence must read back. No standalone CRM proof Boolean exists, and that proof is neither a signature nor go-live approval; **Confirm Authorization** also reads and requires `Go_Live_Approval_Status` not to equal `Approved`. **Record Internal Approval** requires fresh one-time keyed evidence of the exact cryptographically authenticated runtime intent and independently read-back immutable approval receipt, bound to the current Deal, deployment, configuration, route fingerprint, source revision, signed prestate row version, and independently read current poststate version equal to prestate plus one. **Activate Test Route** requires the same prestate/poststate version relation plus a separate fresh one-time keyed receipt proving the exact approval-to-activation event/hash chain, authoritative provider-route readback, active route registry and provider state, one shared route/prestate observation instant, timestamp ordering, and the exact CRM start/seven-day timing. Both signed intent schemas include the exact private CRM Deal ID, enforce the CRM record-ID grammar, and require it to equal the current transition context; activation also verifies that the referenced internal-approval consumption's keyed durable scope binds that same Deal and approval event. Mutable CRM fields alone satisfy neither gate. Only domain-separated keyed digests cross those boundaries; raw private identifiers and receipt/provider values remain outside CRM and Git. HMAC intent authentication is an engineering control, not a legal or electronic signature. Route timestamps, terminal report fields, and Billing evidence are preexisting readback gates, never operator-entered substitutes. Every Closed Lost transition requires `Rollback_Completed_At` and fresh machine-readable external route-inactive evidence. That evidence uses no raw private identifier: domain-separated keyed digests bind the current Deal, deployment, configuration, and authoritative route fingerprint to the rollback and observation timestamps. Nullable deployment/configuration values are HMAC-bound as tagged arrays (`["null"]` versus `["value", value]`) so a literal string can never collide with null. Both route-registry and provider-route state must read inactive after the latest route mutation, the receipt may be at most five minutes old at transition time, and the runtime consumes each nonce once through durable compare-and-set before the transition. Terminal report fields plus fresh current `Deployment_Record_ID` and `Configuration_Version` are all required in authoritative CRM readback; the two current bindings must exactly match the completed summary, while the report contract's nullable fields may remain exactly null. Terminal identity uses authoritative JavaScript canonical-summary bytes with a semantic cross-check, so Python number rendering cannot change the operation identity. Every integer claim is a non-Boolean JavaScript-safe integer, and report/Billing deployment and configuration values use the exact authoritative producer grammars rather than a generic opaque-ID allowance. The non-creating Billing evidence carries an independent keyed receipt over the request action, zero-created-resource claim, complete durable operation, exact CRM/Billing readbacks, and observation time. Its Closed Won readback must carry the private Billing organization identifier, bind it inside the keyed receipt, and match the immutable TEST organization in the catalog context; it is also fixed to `USD`, usage unit `minute`, raw provider status `live`, mapped CRM status `Active`, and the immutable provider-to-CRM map `future` to `Scheduled` and `live` to `Active`. Caller-supplied alternate organization or status semantics fail closed without echoing private identifiers. The bounded source candidate now provides the authenticated CRM callers plus the Catalyst durable receipt/CAS operations for approval, activation, and rollback. Live Blueprint installation, provider save/readback, and ZZZ SYNTHETIC runtime acceptance remain deployment gates rather than source gaps.

The private route-activation validator keeps three separately named, pairwise-distinct keys: `evidence_secret` authenticates only the activation receipt and activation bindings, `approval_evidence_secret` authenticates only the referenced internal-approval durable scope, and `operator_verification_secret` authenticates only the signed activation intent. A wrong approval key fails the approval chain even when the activation receipt is valid. The route-inactive context applies the same CRM Deal record-ID grammar before deriving its private Deal binding.

Closed-Won reconciliation now includes the current Deal `Account_Name` lookup ID and `Results_Review_At` in the keyed exact CRM readback. The validator normalizes the lookup to its CRM record ID and the review datetime to a UTC millisecond instant for private-context comparison, but preserves the exact validated current CRM datetime string in the operation fingerprint to match the JavaScript producer. A reparented Deal, stale review timestamp, or stale operation fingerprint therefore fails closed without exposing the private ID or timestamp.

The historical active-layout gap is computed from every Blueprint criteria, preexisting, operator, and conditional field against the immutable checked-in 2026-08-14 active Deals layout snapshot only; no self-authored release-field union may fill it. The [fresh sanitized preflight](evidence/live-topology-layout-preflight-2026-08-28.json) resolves exactly those 25 API names, including `Billing_Subscription_ID`, `Subscription_Status`, and `Subscription_Start_Date`. The current metadata/layout gate is therefore satisfied, but the readback must be repeated immediately before deployment because live metadata can drift. `Subscription_Start_Date` remains the operator-requested commercial date and is now metadata/layout eligible, while overall operator-input deployment remains blocked with the Blueprint. Billing owns the external subscription start readback, which must exactly match the requested CRM value. Paid activation still requires a fresh non-creating reconciliation, a recomputed exact durable operation fingerprint, and authoritative Billing and CRM readback. The durable operation must predate the provider readback, which may be at most five minutes old when the evidence is assembled. `Subscription_Status = Active`, `Billing_Automation_Status = Paid Verified`, populated customer/subscription/sync evidence, and a read-back empty `Billing_Automation_Error` remain mandatory. The acceptance version is an exact `terms-v1` content address of the private commercial contract.

The last live Blueprint readback remains historical and inactive: all eight states existed, only 11 of 13 required transitions were present, `Record Internal Approval` and `Activate Test Route` were missing, and the legacy combined `Approve Go Live` transition remained. The bounded workflow-repair packet produced a contained partial success: the old-name Form 2 candidate and superseded Form 2 rule were both inactive, while Lead Intake, Controls, and Limits remained active with create-or-edit triggers and Lead Intake still retained one uncontracted scheduled task. Sanitized evidence is recorded in [`evidence/development-workflow-repair-partial-execution-2026-08-29.json`](evidence/development-workflow-repair-partial-execution-2026-08-29.json). The bounded source candidate now includes typed CRM callers plus a private Catalyst durable receipt/CAS controller for separate approval, activation, and rollback. Live Blueprint installation, exact source/configuration readback, and ZZZ SYNTHETIC runtime acceptance remain pending; source completeness is not deployed-state proof.

This contract is desired state, not deployment authorization. The repository topology now respects the five-associated-field-update limit reported by the 2026-08-25 Sylvara workflow-configuration readback. The post-create repair matrix is policy and synthetic test logic only: this repository adds no repair Deluge, callable CRM function, scheduler, or live write. A typed integration writer contract or exact reviewed first-party UI action contract, fresh pre-write read, one bounded group-only write, ambiguous-write containment, exact eight-field readback, and a disposable `ZZZ SYNTHETIC` canary remain mandatory before deployment. The connected workflow writer exposes no reviewed typed payload contract; governed browser fallback is therefore usable only after the exact UI target, controls, save behavior, triggers, rollback, and independent readback are verified. `Type = Initial Sale` must also be supplied before Deal creation because a create-only workflow cannot satisfy the active pre-save validation rule; the repair classifier therefore permits a present exact `Type` while the other Limits defaults are absent, but any non-default value blocks.

## CRM Button Caller Templates

[`config/caller-manifest.json`](config/caller-manifest.json) and [`functions/`](functions/) define five bounded Development callers or controls:

- `Start Free-Test Request` remains the unchanged contained predecessor and rollback fallback.
- `Open Free-Test Setup` reads or initializes the canonical journey on the current Lead or Deal, calls the authenticated Catalyst issue route, and opens only the exact Form 1 URL containing one opaque token and no CRM identity or PII.
- `Issue Revenue Leak Test Setup` retains the controlled Form 2 issuance contract.
- `Approve And Start Free Test` performs approval and activation as two separate backend requests and returns approved/inactive when activation fails.
- `Stop Or Roll Back Free Test` invokes the exact durable stop/unbind/CRM rollback path and displays approved manual provider instructions when automatic restoration is unavailable.

Templates contain placeholders only, never rendered URLs, Connection names, credentials, private identifiers, email addresses, or production values. Rendering occurs privately against one verified Development environment and is never committed or logged. Every caller is Administrator-restricted, performs no automatic remote retry, and emits only constant coarse outcomes. Installation still requires exact compiler/source/button/Blueprint/Connection readback and ZZZ SYNTHETIC canaries; the predecessor remains available until replacement acceptance.

## Private Development Workflow Repair Guard

[`validators/workflow_repair_packet.py`](validators/workflow_repair_packet.py) validates the version-2 single-use private packet for the bounded Development trigger repair without calling CRM or performing a mutation. It binds the committed source revision; the private organization, rule, condition, and action identifiers; fresh evidence windows no longer than 15 minutes; the exact Sylvara CRM Audit and CRM Changes V2 tools; eight ordered main operations; three trigger-only writes; an independent by-ID readback after each write; and a read-only all-five-rule failure-reconciliation gate. The reviewed desired criterion trees apply only to Lead Intake, Controls, and Limits. Form 2 candidate and superseded criteria are accepted only as fresh complete normalized observations labeled non-authoritative; they must remain unchanged through every readback and cannot authorize a rename, action cleanup, or activation. The distinct `crm-workflow-trigger-repair-v2` claim namespace and v2 digest domains prevent the consumed seven-operation contract from being reused as successor authority.

Both JSON inputs must resolve outside every attached Git worktree. Form 2 must independently read back disabled with its submission webhook disabled and zero submissions; the latest sanitized navigation-only observation is [`../../zoho-forms/free-revenue-leak-test/evidence/form2-containment-readback-2026-08-29.json`](../../zoho-forms/free-revenue-leak-test/evidence/form2-containment-readback-2026-08-29.json), but every mutation packet still requires a fresh bounded private prestate. The completed containment step changed only the superseded Form 2 rule's status to inactive with scheduled executions retained; it did not alter that rule's name, module, criteria, or actions, and the inactive candidate was never mutated. Both Form 2 rules must remain inactive while desired Form 2 criterion authority stays blocked until a separately reviewed criterion and controller-receipt contract exists. The executed one-shot packet bound the now-consumed active-superseded prestate and cannot authorize follow-up work. Any remaining trigger repair or scheduled-action deletion requires a fresh validator-bound packet that binds both Form 2 rules inactive; the scheduled-action deletion must remain deferred until the connector-normalized sequence and period fields are represented exactly instead of replaying the rejected provider-shaped payload. The validator rejects any Production, record/customer, Billing, Analytics, Catalyst, Retell, call-routing, Form 2 enablement/submission, or customer-communication action. The validation-only CLI declares but does not enforce single use. The shared local execution boundary revalidates both private files, keeps the validator-returned authority/digest pair internal, and durably claims that exact pair before returning only a coarse receipt; a future provider executor must call that boundary immediately before its first write, stop on every nonzero status, and never retry without a fresh packet and approval.

After committing the reviewed validator source, run it only against private files outside Git:

```powershell
python src/zoho-crm/free-revenue-leak-test/tools/validate_private_workflow_repair_packet.py C:\private\workflow-packet.json C:\private\workflow-approval.json
```

The success output is sanitized and reports `mutationPerformed: false`. A valid result authorizes no action by itself; the exact packet digest still requires its separately recorded single-use approval.

When an approved provider executor exists, its immediate pre-write command is:

```powershell
$env:SYLVARA_APPROVAL_LEDGER_DIRECTORY = 'C:\private\approval-ledger'
python tools/safety/claim_approval_consumption.py crm-workflow-trigger-repair-v2 C:\private\workflow-packet.json C:\private\workflow-approval.json
```

The ledger binding is trusted private executor configuration, not an operation argument, and must remain fixed and access-controlled for the host/account. That command emits only `claimed`, `already-consumed`, or `error`; it accepts no caller-supplied ledger path, authority ID, or digest. Do not run it as a validation substitute because a successful claim permanently consumes the approval even if no provider write follows.

Terminal reports use an automatic, durable handoff without another function or worker mode. The existing `revenue_desk_call_worker` `retry_scan` reads a revision-specific `sync_report_summary` row from `CRMBillingOperations`, sends only its Deal ID and exact operation key to the existing `crm_billing_orchestrator` route using Catalyst `ZCFKEY` plus a distinct report-only credential, and waits for exact CRM and operation-row readback. A scan dispatches at most five report operations. A crashed `report_claim_*` pre-write owner can be safely version-fenced and replaced; after exact `report_write_started_*` readback, ambiguous or conflicting state never repeats the CRM write and converges only through exact Deal readback. Report completion and containment are compare-and-set transitions over the observed status, outcome, and version, so neither stale path can overwrite the other. After observing the operation cursor, every completion transition and completed replay revalidates the authoritative Deal account, deployment/configuration binding, and exact patch. Mismatch or unavailable readback CAS-keeps or demotes reconciliation, and a stale containment loser gets one fresh read plus at most one repair CAS when completion won but the conflict remains.

The summary write maps the authoritative report counts to `Test_Calls_Reaching_Route`, `Test_Qualified_Opportunities`, and `Test_Existing_Customer_Calls`. It does not infer `Test_New_Service_Inquiries`, because the current report has no separate metric with that exact semantic. The orchestrator never writes `Stage` or `Results_Review_At`. After exact summary readback, a human operator owns the valid **Complete Free Test** Blueprint transition only after a fresh current Deal readback reconfirms that `Deployment_Record_ID` and `Configuration_Version` still exactly match the completed summary; the operator later records Results Review, and a normal Deal update is not treated as a Blueprint transition.

The CRM organization is production-type, so acceptance work is restricted to clearly labeled synthetic records. The contract does not authorize Production traffic, real customer/prospect mutation, Zoho Sign, SMS, phone routing, Billing before paid acceptance, or automatic Closed Won.

Live configuration is authoritative only after independent readback. The immutable 2026-08-14 snapshot remains historical evidence and must not be edited to imply a later deployment.

## Local Verification

Run the package-owned contract tests from the repository root:

```powershell
python src/zoho-crm/free-revenue-leak-test/tests/test_contract.py
python src/zoho-crm/free-revenue-leak-test/tests/test_workflow_repair_packet.py
```

Passing tests prove only the checked-in JSON, sanitized Deluge templates, and pure packet-validator behavior. Zoho CRM save/syntax validation, private rendering, Connection behavior, live workflow mutation/readback, route execution, and synthetic acceptance remain separate Development gates.
