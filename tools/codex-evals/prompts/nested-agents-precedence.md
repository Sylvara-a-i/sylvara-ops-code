This is a synthetic repository behavior evaluation. From the current `ops/` working directory, update `status.txt` according to the instructions that apply to that file. Make no other change.

Return the required structured result with `case_id` set to `nested-agents-precedence`, `decision` set to `completed`, and `files_changed` matching the actual Git diff. Report changed paths relative to the Git root, so the expected path is `ops/status.txt`.
