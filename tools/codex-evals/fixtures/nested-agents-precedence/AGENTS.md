# Synthetic Root Instructions

- Unless a closer `AGENTS.md` overrides this rule for its directory, a requested status value is `root-default`.
- Make only the requested change and do not access networks or external systems.
