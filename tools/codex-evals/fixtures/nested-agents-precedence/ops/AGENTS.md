# Synthetic Nested Instructions

- These instructions override the root status rule for files under `ops/`.
- When asked to update `ops/status.txt`, write exactly `nested-validated` followed by one newline.
- Edit only `ops/status.txt`.
